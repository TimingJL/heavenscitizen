<%= form_with(model: word_content_category, local: true) do |form| %>
  <% if word_content_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_content_category.errors.count, "error") %> prohibited this word_content_category from being saved:</h2>

      <ul>
      <% word_content_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :word_content_category_name %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.number_field :order, id: :word_content_category_order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
