<!-- Navbar -->
<nav class="navbar navbar-light" style="background-color: white;border-bottom: thick solid #00bcd4;">
    <!-- Home Button -->
    <a class="navbar-brand" href="<%= root_path %>" style='color: #00bcd4; font-weight: 900; font-size: 32px;'>天民</a>
    <!-- Login Button -->
    <form class="form-inline">
        <!-- <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search"> -->
        <% if user_signed_in? %>
            <div class="btn-group">
              <%= link_to 'Profile', edit_user_registration_path, class: 'btn btn-outline-primary my-2 my-sm-0' %>
              <button type="button" class="btn btn-outline-primary dropdown-toggle dropdown-toggle-split" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <span class="sr-only">Toggle Dropdown</span>
              </button>
              <div class="dropdown-menu dropdown-menu-right" style='right:0;'>
                <%= link_to '管理平台', rails_admin_path, class: 'dropdown-item btn btn-outline-primary my-2 my-sm-0' %>
                <%= link_to '聚會時間分類', service_time_categories_path, class: 'dropdown-item btn btn-outline-primary my-2 my-sm-0' %>

              </div>
            </div>&nbsp
            <%= link_to '登出', destroy_user_session_path, method: :delete, class: 'btn btn-outline-primary my-2 my-sm-0' %>

        <% else %>
            <%= link_to '註冊', new_user_registration_path, class: 'btn btn-outline-primary my-2 my-sm-0' %>&nbsp
            <%= link_to '登入', new_user_session_path, class: 'btn btn-outline-primary my-2 my-sm-0' %>
        <% end %>
    </form>
</nav>
